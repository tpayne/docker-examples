# This docker file creates a centos image and installs various packages into it
FROM centos

# Install a couple of supporting packs and do an update...
RUN yum install zip unzip httpd -y && yum update -y
