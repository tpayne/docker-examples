# Simple webserver
FROM ubuntu:latest

RUN apt-get update 
RUN apt-get install –y apache2 
RUN apt-get install –y apache2-utils 
RUN apt-get clean 

ENTRYPOINT [“apache2ctl”, “-D”, “FOREGROUND”]
EXPOSE 80 
